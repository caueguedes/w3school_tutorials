// + Addition
// - subtraction
// * multiplication
// ** exponential
// / division
// % Modulus
// ++ increment
// -- decrement

var x = 100;
var y = 50;

// Adding
var z = x + y;

// Subtracting
var z = x - y;

// Multiplying
var z = x * y;

// Dividing
var z = x / y;

// Remainder
var z = x % y;

// Increment
x++;

// Decrement
x--;

// Exponential
var z = x ** 2;          // result is 25


// &	  Bitwise     AND	  x & y
// ^	  Bitwise     XOR	  x ^ y
// |	  Bitwise     OR	  x | y

// &&	Logical     AND	  x && y
// ||	Logical     OR	  x || y
// ? 	Condition         ? "Yes" : "No"
